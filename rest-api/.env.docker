APP_NAME="BRAC Microfinance API"
APP_ENV=local
APP_KEY=base64:ZqV+9YmZGGGjBpJZGGGjBpJZGGGjBpJZGGGjBpJZGGGjBpJZGGG=
APP_DEBUG=true
APP_URL=http://localhost:8000

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration (Docker MySQL)
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=microfinance_db
DB_USERNAME=microfinance_user
DB_PASSWORD=microfinance_password

# Redis Configuration (Docker Redis)
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Cache Configuration
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120

# Broadcasting
BROADCAST_DRIVER=log

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# AWS (for production)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher (for real-time features)
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# JWT Configuration
JWT_SECRET=your-very-long-secret-key-for-jwt-tokens-minimum-256-bits-required
JWT_ALGO=HS256
JWT_TTL=60

# Microfinance Specific Configuration
SEED_DATABASE=true
DEFAULT_BRANCH_ID=1
MAX_LOAN_AMOUNT=500000
MIN_LOAN_AMOUNT=5000
DEFAULT_INTEREST_RATE=12.5

# Development Tools (Docker services)
PHPMYADMIN_PORT=8080
REDIS_COMMANDER_PORT=8081